<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="../NetIndex.Core.Abstractions/NetIndex.Core.Abstractions.csproj" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Options" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="NetIndex.Core.Tests" />
  </ItemGroup>

  <PropertyGroup>
    <Title>NetIndex Core</Title>
    <Description>Core pipeline orchestration for NetIndex: wires together embedding, vector search, chunking, and generation into the INetIndexPipeline lifecycle.</Description>
    <PackageTags>rag;netindex;core;pipeline</PackageTags>
  </PropertyGroup>
</Project>
